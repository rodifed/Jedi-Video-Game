name: Build Cross-Platform Executables

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  workflow_dispatch: {}

jobs:
  build-windows:
    runs-on: windows-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: '3.x'

      - name: Install dependencies
        run: |
          pip install -r requirements.txt

      - name: Build Windows executable with PyInstaller
        run: |
          pyinstaller --onefile --add-data "sounds/*;sounds" app.py

      - name: Upload Windows executable
        uses: actions/upload-artifact@v3
        with:
          name: windows-exe
          path: dist/app.exe

  build-macos:
    runs-on: macos-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: '3.x'

      - name: Install dependencies
        run: |
          pip install -r requirements.txt

      - name: Build macOS app with PyInstaller
        run: |
          pyinstaller --onefile --add-data "sounds/*:sounds" app.py

      - name: Upload macOS app
        uses: actions/upload-artifact@v3
        with:
          name: macos-app
          path: dist/app.app

  build-linux:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: '3.x'

      - name: Install dependencies
        run: |
          pip install -r requirements.txt

      - name: Build Linux executable with PyInstaller
        run: |
          pyinstaller --onefile --add-data "sounds/*:sounds" app.py

      - name: Upload Linux binary
        uses: actions/upload-artifact@v3
        with:
          name: linux-binary
          path: dist/app
